<?php require_once __DIR__."/../config/database.php";require_once __DIR__."/../includes/functions.php";header("Content-Type: application/json");if(!isLoggedIn()){echo json_encode(["error"=>"Unauthorized"]);exit;}$id=(int)($_GET["id"]??0);if(!$id){echo json_encode(["error"=>"Invalid"]);exit;}$s=dbQueryOne("SELECT s.*,p.nama as pnama FROM surat s JOIN prodi p ON p.id=s.prodi_id WHERE s.id=?",[$id]);echo $s?json_encode($s):json_encode(["error"=>"Not found"]);
